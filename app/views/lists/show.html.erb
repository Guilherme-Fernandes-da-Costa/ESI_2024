<h1><%= @lista.nome %></h1>

<ul>
  <% @lista.items.each do |item| %>
    <li>
      <%= item.name %>
      <% item.tags.each do |tag| %>
        <span class="item-tag"><%= tag.name %></span>
      <% end %>
    </li>
  <% end %>
</ul>

<%= link_to "Novo Item", new_lista_item_path(@lista) %>
