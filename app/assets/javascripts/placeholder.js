// Placeholder file to ensure the legacy Sprockets `app/assets/javascripts`
// directory exists in the repository. Some gems (e.g., jasmine-rails)
// expect this path to be present; having at least one file prevents
// Sprockets::FileNotFound errors in CI.

// No runtime code required.
